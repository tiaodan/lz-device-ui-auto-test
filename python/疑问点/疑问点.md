1 login_ok 断言如何定位元素的？xpath吗
	- 不是。用的css选择器
2 是否需要绝对定位，固定位置写法，判断元素是否存在
	- 一般不需要，它会引发更多的问题！并且，断言还不好弄
3 是否需要依次测试， 元素不存在，或者多元素，位置不对，白屏等情况？看看自己模拟的错误界面，能否检测到？
	- 还没回答。要再问！！！！
4. 封装常用方法。
5. 考虑搭建框架，把xpath相关，写成配置文件。判断等方法，写成通用方法
6. 白屏时还能定位到元素吗？
- 不能